export interface PresentItem {
  id: string;
  title: string;
  subtitle: string;
  imageUrl: string;
}

export interface PresentProject {
  id: string;
  name: string;
  status: string;
  startDate: string;
  endDate: string;
}